import React from 'react';

export const metadata = {
  title: 'Performance Dashboard - Real-Time Canvas Visualization',
  description: '60 FPS 100k Point Telemetry Visualization Dashboard built from scratch',
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
